export declare const generator: {
    aooFactory: (snapshot: any[], amount: number) => any[];
    arrayGenerator: (reserve: number) => any;
    objectGenerator: (reserve: number) => any;
    random: (max?: number) => number;
    randomElement: (reserve: number) => any;
};
